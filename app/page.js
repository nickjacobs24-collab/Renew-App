export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <p className="text-sm text-white/40">
        Prism — rebuild in progress. Beats land here per CLAUDE.md §4.
      </p>
    </main>
  );
}
